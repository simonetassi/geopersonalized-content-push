apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: repo-birdland
spec:
  serviceName: "service-birdland"
  replicas: 1
  selector:
    matchLabels:
      app: content-repo
      venue_name: birdland
  template:
    metadata:
      labels:
        app: content-repo
        venue_name: birdland
    spec:
      containers:
      - name: content-repo
        image: geo-app/content-repo:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
        volumeMounts:
        - name: storage
          mountPath: /app/storage
  volumeClaimTemplates:
  - metadata:
      name: storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: service-birdland
spec:
  ports:
  - port: 80
    targetPort: 3000
  selector:
    venue_name: birdland