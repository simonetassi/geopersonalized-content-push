apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: repo-central-park
spec:
  serviceName: "service-central-park"
  replicas: 1
  selector:
    matchLabels:
      app: content-repo
      venue_name: central-park
  template:
    metadata:
      labels:
        app: content-repo
        venue_name: central-park
    spec:
      containers:
      - name: content-repo
        image: geo-app/content-repo:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
        volumeMounts:
        - name: data
          mountPath: /app/storage
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: service-central-park
spec:
  ports:
  - port: 80
    targetPort: 3000
  selector:
    venue_name: central-park